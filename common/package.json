{
  "name": "@easy-show-downloader/common",
  "private": true,
  "type": "module",
  "packageManager": "yarn@4.13.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "lint": "eslint src",
    "test": "NODE_ENV=test mocha --require ts-node/register",
    "common:build": "tsc",
    "common:clean": "rm -rf dist tsconfig.tsbuildinfo",
    "common:lint": "eslint src",
    "common:test": "NODE_ENV=test mocha --require ts-node/register"
  },
  "devDependencies": {
    "@types/chai": "^5.2.3",
    "@types/eslint": "^9.6.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.1.2",
    "chai": "^6.2.2",
    "eslint": "^10.8.0",
    "mocha": "^11.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
