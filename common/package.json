{
  "name": "@easy-show-downloader/common",
  "private": true,
  "packageManager": "yarn@3.2.0",
  "version": "2.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -r dist",
    "lint": "eslint src --ext ts",
    "test": "NODE_ENV=test ts-mocha",
    "common:build": "tsc",
    "common:clean": "rm -r dist",
    "common:lint": "eslint src --ext ts",
    "common:test": "NODE_ENV=test ts-mocha"
  },
  "devDependencies": {
    "@types/chai": "^4",
    "@types/eslint": "^8",
    "@types/mocha": "^9",
    "chai": "^4.3.4",
    "eslint": "^8.3.0",
    "eslint-config-google": "^0.14.0",
    "mocha": "^9.1.3",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.5.2"
  }
}
